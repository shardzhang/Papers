

FTRL在工业界的推广

FTRL算法真正被广泛熟知和应用，主要归功于Google团队在2010年代初将其成功应用于广告点击率（CTR）预估这一大规模工业场景。他们发表的这篇实践性论文让FTRL成为业界处理稀疏大规模在线学习问题的经典算法。

值得一提的是，到了2024-2025年，关于FTRL的研究仍然非常活跃，学术界在不断探索其在更复杂场景下的自适应学习率等问题。



2013-Ad Click Prediction-a View from the Trenches-翻译.md中的 

对于大规模学习，广义线性模型（如逻辑回归）的在线算法具有许多优势。尽管特征向量 $x$ 可能有数十亿维度，但通常每个实例只有数百个非零值。这使得通过从磁盘或网络流式传输样本即可在大规模数据集上进行高效训练成为可能 [3]，因为每个训练样本只需要被处理一次。 

这段话是什么意思？



这段话解释的是**广义线性模型在工业级规模下的实用性优势**：

1. **特征极高维但稀疏** — 广告点击预估中特征向量可能有数十亿维（如用户 ID × 广告 ID 交叉），但单个样本通常只有几百个非零特征（绝大多数是 0）。这种**天然稀疏性**使得模型只需存储和处理非零特征，计算量不随总维度增长。

2. **Online 学习 = 一次遍历** — 样本流式读取，处理完即可丢弃，不需要像 batch 方法那样反复扫描数据集。对于 PB 级日志数据，"只过一遍"是唯一可行方案（内存/磁盘都存不下全部数据）。

3. **线性模型计算量极低** — 逻辑回归的梯度计算 $\nabla w = (p - y)x$，对于稀疏 $x$ 只需更新非零权重，一步 O(非零特征数)。深度网络则需前向+反向传播全连接，计算量高几个数量级，无法在同等数据量下以低成本训练。

一句话总结：**特征稀疏 + 线性模型 + online 更新 = 单次扫描即可训练，内存和计算都只跟非零特征数相关，与总维度无关**，这让数十亿维度、万亿样本的训练变得可行。



batch_size越大越好么？还是越小越好？



都不是，存在 tradeoff：是梯度估计准确性、训练速度、显存开销/利用率之间的权衡

**batch size 太小（如 1）**：

- GPU现存开销小
- GPU硬件利用率低
- 梯度噪声大，收敛不稳定
- 训练速度慢

**batch size 太大**：

- 梯度估计准确，收敛稳定
- 内存/显存占用高，对于大内存显卡的利用率高
- 训练速度快





## Algorithm 1 详解

$$
\eta_{t,i} = \frac{\alpha}{\beta + \sqrt{\sum_{s=1}^t g_{s,i}^2}} \qquad (2)
$$

这是 FTRL-Proximal 算法用于逻辑回归的每坐标实现，结合 L1 + L2 正则化。

### 输入参数
- `α, β`：控制每坐标学习率（参见公式 2: $\eta_{t,i} = \alpha/(\beta + \sqrt{n_{t,i}})$）
- `λ1, λ2`：L1、L2 正则化强度

### 初始化
对所有特征 $i$：$z_i = 0,\ n_i = 0$（只需存两个数/特征）

### 每轮训练循环

**Step 1 — 计算当前权重 $w_{t,i}$**：

```
wt,i = 0                                         若 |zi| ≤ λ1
wt,i = -( (β+√ni)/α + λ2 )^{-1} (zi - sgn(zi)λ1)  否则
```

- 若 $|z_i| \leq \lambda_1$ → 权重被**硬截断为 0**（L1 正则化产生稀疏性的关键）
- 否则按闭式解计算非零权重，其中 $(\beta+\sqrt{n_i})/\alpha + \lambda_2$ 是 L2 正则化 + 学习率缩放项

**Step 2 — 预测**：
$p_t = \sigma(w_t \cdot x_t)$（sigmoid 得到点击概率）

**Step 3 — 观察到真实标签 $y_t$**

**Step 4 — 更新每个非零特征的 $z_i, n_i$**：

- $g_i = (p_t - y_t) x_i$ — 逻辑损失对 $w_i$ 的梯度
- $\sigma_i = \frac{1}{\alpha}(\sqrt{n_i + g_i^2} - \sqrt{n_i})$ — 相当于 $1/\eta_{t,i} - 1/\eta_{t-1,i}$，即学习率倒数的变化量
- $z_i \leftarrow z_i + g_i - \sigma_i w_{t,i}$ — 累积"带负学习率缩放权重的梯度总和"
- $n_i \leftarrow n_i + g_i^2$ — 累积梯度平方，用于下一轮的每坐标学习率

### 核心设计思想

| 组件                            | 作用                                                         |
| ------------------------------- | ------------------------------------------------------------ |
| **$n_i$（梯度平方和）**         | 控制每坐标学习率：频繁出现的特征 $n_i$ 大 → 学习率小 → 权重稳定 |
| **$z_i$（累积状态）**           | 等价于 OGD 的 $-w$，但加入了 L1 截断机制                     |
| **$|z_i| \leq \lambda_1$ 截断** | 产生稀疏性的关键：当 $z_i$ 不够大时，权重直接置 0，不再更新  |
| **每坐标学习率**                | 每个特征独立的学习率，稀疏特征保持高学习率，密集特征快速衰减 |

为什么比 OGD 更好？

OGD 更新 $w_{t+1} = w_t - \eta_t g_t$，在 L1 正则化下难以产生精确的零值。FTRL-Proximal 改用**惰性表示**（存储 $z$ 而非 $w$），在 $w$ 上施加显式 L1 截断门限，每次更新时直接做 **soft thresholding**：$|z_i| \leq \lambda_1 \Rightarrow w_i = 0$，从而获得高稀疏性且不牺牲精度。





在线梯度下降的标准理论建议使用全局学习率调度 $\eta_t = 1/\sqrt{t}$，这**对所有坐标都是通用的** [34]。 

「每坐标」是什么意思？



"每坐标"就是**每个特征维度独立拥有自己的学习率**，而非所有特征共用一个全局学习率。

用论文里的抛硬币例子解释：

- **全局学习率** $\eta_t = 1/\sqrt{t}$：不管第 $t$ 步更新的是哪个特征，所有 $d$ 个维度的学习率都按同一个时间表下降。即使特征 $i$ 这轮没被更新，它的学习率也在降低。
- **每坐标学习率** $\eta_{t,i} = \alpha/(\beta + \sqrt{n_{t,i}})$：每个特征 $i$ 根据**自己**被更新的次数 $n_i$ 独立计算学习率。$n_i$ 大（出现频繁）→ 学习率小（已充分学习）；$n_i$ 小（罕见特征）→ 学习率大（需要快速适应）。

**直观类比**：全局学习率像一个班级按统一进度上课；每坐标学习率像每个学生按自己的掌握速度独立学习——频繁出现的特征（学了很多遍）放慢节奏，罕见特征（刚遇到）保持高学习率快速吸收。







3. 在线学习与稀疏性

这部分的公式我是一点没看懂



好的，我用人话逐条解释这些公式到底在做什么。

---

**公式 1 — LogLoss（逻辑损失）**

$$
\ell_t(w_t) = -y_t \log p_t - (1 - y_t) \log(1 - p_t)
$$

- $y_t$ 是真实标签（1=点击了，0=没点）
- $p_t$ 是模型预测的点击概率（比如 0.7）

这个公式量化：**当模型说概率是 $p$ 而实际是 $y$ 时，错得有多离谱**。
- 如果用户点了（$y=1$）：损失 = $-\log p$。模型说概率 0.9 → 损失很小；说 0.1 → 损失巨大。
- 如果没点（$y=0$）：损失 = $-\log(1-p)$。模型说概率 0.1 → 损失很小；说 0.9 → 损失巨大。

就是：**预测越离谱，损失越大**。

---

**公式 — 梯度（求导结果）**

$$
\nabla \ell_t(w) = (p_t - y_t) x_t
$$

- $p_t - y_t$ = 预测误差：点了就是 $p - 1$（负数），没点就是 $p - 0$（正数）
- $x_t$ 是特征向量

梯度告诉你 **"权重该往哪个方向调"**：
- 点了但预测低（$p=0.3, y=1$）：误差 $-0.7$，梯度指向负方向 → 下次调大权重让预测更高
- 没点但预测高（$p=0.8, y=0$）：误差 $+0.8$，梯度指向正方向 → 下次调小权重让预测更低

**一句话**：误差的正负决定了权重的调整方向。

---

**公式 — OGD 更新**

$$
w_{t+1} = w_t - \eta_t \cdot g_t
$$

- $w_t$ = 当前权重
- $g_t$ = 当前梯度（告诉你怎么调）
- $\eta_t$ = 学习率（控制调多大步）

逻辑：$w_{\text{新}} = w_{\text{旧}} - 学习率 \times 梯度$。如果梯度为正（预测高了），减去它让权重下降；梯度为负（预测低了），减去负数等于加上正数让权重上升。

---

**公式 — FTRL-Proximal（核心目标函数）**

$$
w_{t+1} = \arg\min_w \left( g_{1:t} \cdot w + \frac{1}{2} \sum_{s=1}^t \sigma_s \|w - w_s\|^2 + \lambda_1 \|w\|_1 \right)
$$

这个式子看起来很吓人，实际上拆开看：

| 部分                                     | 人话                                                         |
| ---------------------------------------- | ------------------------------------------------------------ |
| $g_{1:t} \cdot w$                        | 新的 $w$ 在所有历史数据上表现怎样（最小化累积损失）          |
| $\frac{1}{2}\sum \sigma_s \|w - w_s\|^2$ | **"别跑太远"**：新权重不要和每步的旧权重偏离太多（陡坡稳定） |
| $\lambda_1 \|w\|_1$                      | **L1 惩罚**：鼓励权重等于精确的 0（产生稀疏性，节省内存）    |

每一轮它做的事就是：**"在历史数据上拟合得好的前提下，权重尽量不剧烈变化，同时尽可能把不重要的权重压成 0。"**

---

**公式 — 简化版 $w$ 更新（每坐标）**

$$
w_{t+1,i} =
\begin{cases}
0 & \text{if } |z_{t,i}| \leq \lambda_1 \\
-\eta_t (z_{t,i} - \operatorname{sgn}(z_{t,i}) \lambda_1) & \text{otherwise}
\end{cases}
$$

这其实就是上面那个又长又吓人的公式解出来的**闭式解**，实际代码里算的就是这个。

- $z_{t,i}$ 是一个累积的"中间变量"（可以理解为带衰减的负梯度累积和）
- 如果 $|z_i|$ 很小（小于 $\lambda_1$），那么 $w_i$ **直接设成 0** → 这就是**产生稀疏性的核心机制**
- 否则 $w_i$ 取非零值，并且从 $z_i$ 中减去 $\lambda_1$（这叫 **soft-thresholding**，软阈值）

**类比**：想象 $z_i$ 是"这个特征有用程度的证据"。证据不够强（$|z_i| \leq \lambda_1$）就认为它没用，权重砍到 0 彻底丢弃它。

---

**公式 — 每坐标学习率**

$$
\eta_{t,i} = \frac{\alpha}{\beta + \sqrt{\sum_{s=1}^t g_{s,i}^2}}
$$

- 分子 $\alpha$：基础学习率（调这个控制整体大小）
- 分母 $\sqrt{\sum g_{s,i}^2}$：历史上这个特征梯度大小的**平方根累积**
  - 特征频繁出现 → $g_i$ 累积大 → 分母大 → 学习率小 → 稳定收敛
  - 特征很少出现 → $g_i$ 累积小 → 分母小 → 学习率大 → 快速适应新数据

**直觉**：每个特征自己记账——"我来过几次？"来得多的特征学习率小（差不多学会了），来得少的特征学习率大（见到一次多学点）。
